The ``Mylist`` module
======================

Using `Mylist``
-------------------

Importing the function from the module:
	>>> Mylist = __import__("1-my_list").Mylist

Checking for module docstring:
	 >>> m = __import__("1-my_list").__doc__
	 >>> len(m) > 1
	 True

Checking for function docstring:
	 >>> f = __import__("1-my_list").Mylist.__doc__
         >>> len(f) > 1
         True

Checking for PEP8:
	 >>> import os
	 >>> os.popen("pep8 1-my_list.py").read()
	 ''

Checking for last blank line:
	 >>> os.popen("cat -e 1-my_list.py | tail -1").read()[-1]
	 '\n'
Checking Adding elements:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)

Checking print:
     >>> print(my_list)
    [1, 4, 2, 3, 5]

Cheking negative integer:
    >>> my_list.append(-1)
    >>> my_list.print_sorted()
    [-1, 1, 2, 3, 4, 5]

Checking A sorted list, print and printing with the method:
    >>> my_list = MyList([1, 5, 15, 25])
    >>> print(my_list)
    [1, 5, 15, 25]
    >>> my_list.print_sorted()
    [1, 5, 15, 25]

Checking an empty list:
    >>> my_list = MyList()
    >>> print(my_list)
    []
    >>> my_list.print_sorted()
    []

Checking All negatives:
	>>> list1 = MyList()
	>>> list1.extend([-10, -2, -100, -900, -50])
	>>> list1.print_sorted()
	[-900, -100, -50, -10, -2]
